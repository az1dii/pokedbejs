<!DOCTYPE html>
<html lang="en">

<head>
    <%- include ('./partials/head.ejs') %>
</head>

<body>
    <div class="headline">
        <h2 style="color:rgba(9, 9, 9)">Pokemon Show Page</h2>
        <a href="/pokemon" class="button" style="color:rgb(65, 196, 189)">Back</a>
    </div>
    <div class="container">
        <table>
            <tr>
                <th>Name</th>
                <td><%= pokemon.name%></td>
                <td><img src="<%= pokemon.img%>" alt="pokemon image" height='100' width='100'></td>
            </tr>

            <tr>
                <th>Type</th>
                <% for (let i = 0; i < pokemon.type.length; i++) { %>
                <td><%= pokemon.type[i]%></td>
                <% } %>
            </tr>

            <tr>
                <th>Stats</th>
                <% for (let key in pokemon.stats) {%>
                <td><%= key%>: <%= pokemon.stats[key]%> </td>
                <%}%>
            </tr>
        </table>
    </div>

</body>

</html>